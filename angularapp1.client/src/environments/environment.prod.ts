//export const environment = {
//  production: true,
//  apiBase: (typeof window !== 'undefined') ? window.location.origin : ''
//};
export const environment = {
  production: true,
  apiBase: '/api/v1' 
};
